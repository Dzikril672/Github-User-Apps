<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dicoding.mysubmission2">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_git"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_git_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MySubmission2">
        <activity
            android:name=".activity.FavoritActivity"
            android:exported="true" />
        <activity
            android:name=".ui.setting.SettingActivity"
            android:exported="true" />
        <activity
            android:name=".ui.setting.SplashScreenActivity"
            android:exported="true"
            android:theme="@style/splashtheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.details.DetailActivity"
            android:exported="true" />
        <activity
            android:name=".ui.main.MainActivity"
            android:exported="true" />
    </application>

</manifest>